#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
   // printf() displays the string inside quotation
   printf("Hello, Wisdoms!");
   return 0;
}

